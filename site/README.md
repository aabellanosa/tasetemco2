# ACME Cooperative Static Documentation Site

Upload the contents of this folder to the document root of the Namecheap subdomain.

Expected files:

- `index.html`
- `styles.css`

This static site does not need Node.js, SQLite, or environment variables.
