{
  "name": "tasetemco",
  "version": "0.1.0",
  "private": true,
  "description": "TASETEMCO prototype with a small Node.js and SQLite backend.",
  "main": "server.js",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.js",
    "test": "node scripts/smoke-app.js",
    "smoke": "node scripts/smoke-app.js",
    "check": "node --check server.js && node --check src/db.js && node --check app.js && node --check scripts/smoke-app.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
