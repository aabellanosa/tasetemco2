{
  "name": "tasetemco",
  "version": "0.1.0",
  "private": true,
  "description": "TASETEMCO cooperative system prototype spike.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "db:schema": "node scripts/db-maintenance.js schema",
    "db:seed": "node scripts/db-maintenance.js seed",
    "db:backup": "node scripts/db-maintenance.js backup",
    "db:reset-demo": "node scripts/db-maintenance.js reset-demo",
    "test": "node scripts/smoke-spike.js",
    "smoke": "node scripts/smoke-spike.js",
    "smoke:mysql": "node scripts/smoke-mysql.js",
    "check": "node --check backend/src/server.js && node --check backend/src/data.js && node --check scripts/smoke-spike.js && node --check scripts/smoke-mysql.js && node --check scripts/db-maintenance.js && npm --prefix frontend run check"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
