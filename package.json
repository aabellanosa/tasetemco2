{
  "name": "tasetemco",
  "version": "0.1.0",
  "private": true,
  "description": "ACME Cooperative workflow reference documentation.",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/build-docs.js",
    "test": "node scripts/smoke-docs.js",
    "start": "node --disable-warning=ExperimentalWarning server.js",
    "check": "node --check server.js && node --check src/db.js && node --check app.js && node --check scripts/build-docs.js && node --check scripts/smoke-docs.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
