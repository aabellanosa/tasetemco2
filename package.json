{
  "name": "tasetemco",
  "version": "0.1.0",
  "private": true,
  "description": "TASETEMCO prototype with a small Node.js and SQLite backend.",
  "main": "server.js",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.js",
    "check": "node --check server.js && node --check src/db.js && node --check app.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
